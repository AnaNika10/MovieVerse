// External libraries
import React from 'react';

// UI components
import { Typography } from '@mui/material';

export default class AppComponent extends React.Component {
	render() {
		return (
			<Typography variant='h2' color='textPrimary'>MovieVerse</Typography>
		);
	}
}
