import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: '#111116',
		},
		secondary: {
			main: '#1d1d24',
		},
		background: {
			paper: '#111116',
		},
		text: {
			primary: '#ffffff',
			secondary: '#000000',
		},
	},
});

export default theme;
